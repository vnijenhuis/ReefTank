@{
    ViewBag.Title = ViewBag.Title;
    Layout = "~/Views/Shared/_BaseLayout.cshtml";
}

@section Head {
    @RenderSection("Head", false)
}

@section Menu {
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Home", "Index", "Home", new { Area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li><a href="@Url.Action("Index","Home", new { Area = "Fish"})">Fish</a></li>
                    <li><a href="@Url.Action("Index","Home", new { Area = "Corals"})">Corals</a></li>
                    @*<li><a href="@Url.Action("About","Home", new { Area = ""})">About</a></li>
                    <li><a href="@Url.Action("Contact","Home", new { Area = ""})">Contact</a></li>*@
                </ul>
            </div>
        </div>
    </div>
}

@RenderBody()

@section Scripts {
    @RenderSection("Scripts", false)
}